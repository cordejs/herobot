#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@google-cloud/functions-emulator/bin/functions" "$@"
  ret=$?
else 
  node  "$basedir/../@google-cloud/functions-emulator/bin/functions" "$@"
  ret=$?
fi
exit $ret
